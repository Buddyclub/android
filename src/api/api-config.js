import defaulUrl from '../config/url';

/**
 * The options used to configure the API.
 */

/**
 * The default configuration for the app.
 */
export const DEFAULT_API_CONFIG = {
  url: defaulUrl.baseUrl,
  timeout: 10000,
};
